
guard clicking accross all buttons
# npx cap sync after every npm run build
npx cap run android

Raw.jsx on mobile browsers keeps ending users session and theyd have to start process all over again and then welcome back catches them, i want to prevent this from happening,
seems whats happening is that token keeps expiring, why? nd what is making token to expire? token should be refreshed even before it expires and its lifetime is 30 days, session ttl is 60 days of inactivity